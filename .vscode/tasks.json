{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compila C",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.exe",
                "-lm"
            ],
            "group": "build"
        }
    ]
}